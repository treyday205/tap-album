version: '3.9'
services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: tap
      POSTGRES_PASSWORD: tap
      POSTGRES_DB: tap
    ports:
      - "5432:5432"
    volumes:
      - tap_pg_data:/var/lib/postgresql/data
volumes:
  tap_pg_data:
